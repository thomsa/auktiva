{
  "buildCommand": "prisma generate && npx tsx scripts/migrate-turso.ts && next build",
  "installCommand": "npm ci",
  "framework": "nextjs",
  "outputDirectory": ".next",
  "crons": [
    {
      "path": "/api/cron/retry-emails",
      "schedule": "0 1 * * *"
    }
  ]
}
